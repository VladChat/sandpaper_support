{
  "name": "sandpaper-support",
  "private": true,
  "version": "0.1.0",
  "description": "Problem-first public support system for eQualle sandpaper products.",
  "scripts": {
    "validate:data": "node scripts/build-solution-pages.js --check",
    "build:content": "node scripts/build-solution-pages.js --check",
    "build:solution-pages:check": "node scripts/build-solution-pages.js --check",
    "build:solution-pages": "node scripts/build-solution-pages.js --write",
    "build:solution-pages:preview": "node scripts/build-solution-pages.js --preview uneven-pressure-pattern"
  }
}
